// Keep existing regression fixtures identical to the published contributor testkit.
export { universe, snapshot, settings, revise, regressionSuiteFixture, scopeFixture, evaluatedFixture, fixtures } from '../../src/search/testing.js'
